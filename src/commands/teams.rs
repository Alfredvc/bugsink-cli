use anyhow::Result;
use crate::cli::TeamsCommands;
use crate::output::Output;

pub async fn run(_command: &TeamsCommands, _output: &Output, _url: Option<&str>, _token: Option<&str>, _all: bool) -> Result<()> {
    todo!()
}
