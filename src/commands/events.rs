use anyhow::Result;
use crate::cli::EventsCommands;
use crate::output::Output;

pub async fn run(_command: &EventsCommands, _output: &Output, _url: Option<&str>, _token: Option<&str>, _all: bool) -> Result<()> {
    todo!()
}
