use anyhow::Result;
use crate::cli::ProjectsCommands;
use crate::output::Output;

pub async fn run(_command: &ProjectsCommands, _output: &Output, _url: Option<&str>, _token: Option<&str>, _all: bool) -> Result<()> {
    todo!()
}
